apiVersion: apps/v1
kind: Deployment
metadata:
  name: asset-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: asset-api
  template:
    metadata:
      labels:
        app: asset-api
    spec:
      containers:
      - name: asset-api
        image: yourdockerhub/asset-api:latest
        ports:
        - containerPort: 8080